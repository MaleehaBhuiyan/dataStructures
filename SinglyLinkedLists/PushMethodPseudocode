Push Pseudocode: adding a new node to the end of the linked list 

• This function should accept a value 
• Create a new node using the value passed to the funciton 
• If there is no head property on the list, set the head and the tail to be the newly created node 
• Otherwise set the next property on the tail to be the new node and set the tail property on the list to be the newly created node 
• Increment the length by one
• Return the linked list   

